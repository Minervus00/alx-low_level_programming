#include <stdio.h>

/**
 * main - buhbhbh
 *
 * Return: (0) ggh
 */
int main(void)
{
	char alpha[] = "abcdefghijklmnopqrstuvwxyz";
	int i;

	for (i = 25; i >= 0; i--)
		putchar(alpha[i]);
	putchar('\n');

	return (0);
}
