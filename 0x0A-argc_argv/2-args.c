#include <stdio.h>

/**
 * main - ooo
 * @argc: oiid
 * @argv: jsdd
 *
 * Return: intt
 */
int main(int argc, char __attribute__((__unused__)) *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
		printf("%s\n", argv[i]);

	return (0);
}
