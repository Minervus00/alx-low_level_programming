#include "main.h"
#include <string.h>

/**
 * _strncpy - thattt
 * @dest: destination
 * @src: the src
 * @n: bytes
 *
 * Return: char *
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
